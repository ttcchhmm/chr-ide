{
    "name": "@chr-ide/simple-client",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build": "tsc",
        "dev": "npm run build && node dist/main.js",
        "clean": "rimraf dist tsconfig.tsbuildinfo"
    },
    "description": "A simple client to test the backend.",
    "dependencies": {
        "socket.io-client": "^4.8.1",
        "@chr-ide/core": "0.0.0"
    }
}
